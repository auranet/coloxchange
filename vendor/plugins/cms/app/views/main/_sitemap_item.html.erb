<li>
  <%= link_to page.name,:controller => "main",:action => "index",:id => page.slug %><% unless page.children.empty? %>
  <ul><% for child in page.children %>
    <%= render :partial => "sitemap_item",:locals => {:page => child} %><% end %>
  </ul><% end %>
</li>